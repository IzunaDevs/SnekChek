language: python

python:
  - "3.6"

install:
  - "pip install -r requirements-ci.txt"

script:
  # ignore output from install
  - python setup.py install >> /dev/null
  - snekchek
  - snekchek --json

cache: pip

notifications:
  email: false
